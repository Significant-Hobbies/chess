logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

node_modules
dist
dist-ssr
.env*
*.local

.DS_Store
.vercel

# fallow cache
.fallow/
cache.bin

# Blume (docs presentation layer) — generated runtime + build output.
# Markdown in docs/ is the source of truth; these are derived.
.blume/
.blume-verify/

# Local agent logs / scratch (not agent config/skills dirs — those may be committed)
agent.log
*.agent.log
agents.log
