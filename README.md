# Chess

## Setup

```bash
pnpm install
cp .env.example .env  # fill in values
pnpm dev
```

## Scripts

- `pnpm dev` — `cd server && npm install --silent && node index.mjs & vite`
- `pnpm build` — `vite build`

## Notes

See `AGENTS.md` / `agents.md` for architecture.
